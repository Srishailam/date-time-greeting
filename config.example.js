// Configuration file for the Date Time Greeting Extension
// No API key needed! Using free Open-Meteo weather service

const CONFIG = {
    // Location detection method:
    // 'geolocation' - Browser GPS/WiFi location (most accurate, requires permission)
    // 'ip' - Automatic location detection using IP address (fast, city-level accuracy)
    LOCATION_METHOD: 'geolocation',
    
    // Temperature unit: 'fahrenheit' or 'celsius'
    TEMPERATURE_UNIT: 'fahrenheit'
};

